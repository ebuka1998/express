let jwt,config,devConfig;_500‍.x([["authorization",()=>authorization],["authorize",()=>authorize]]);_500‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_500‍.w("./config",[["default",["config"],function(v){config=v}]]);_500‍.w("./config/dev",[["default",["devConfig"],function(v){devConfig=v}]]);



/**validate access token */
       const authorization = (req, res, next) => {
    const token = req.header('auth-token')
    if(!token) {
        return res.status(401).send('you must sign in first')
    }

    try {
        const userInfo = jwt.verify(token, config.secrets.JWT_SECRET)
        req.user = userInfo
        next()
    } catch (error) {
        return res.status(400).send('your token is invalid or has expired')
    }
}

       const authorize = (req, res, next) => {
    const token = req.query.token
    if(!token) {
        return res.status(401).send('you must sign in first')
    }

    try {
        const userInfo = jwt.verify(token, config.secrets.JWT_SECRET)
        req.user = userInfo
        next()
    } catch (error) {
        return res.status(400).send('your token is invalid or has expired')
    }
}